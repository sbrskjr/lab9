#ifndef TASKS_H
#define TASKS_H

#include <iostream>
#include <string>
#include <fstream>

void task1();
void task2();
void my_insert(char* str, int pos, const char* s);
bool is_valid(const std::string& str);
void process_string(std::string& s, const std::string& s1, const std::string& s2);

#endif // TASKS_H
